# Backlog de correções — achados colaterais

> Durante uma tarefa você percebe algo errado/melhorável que NÃO é a tarefa
> atual: registre aqui e siga — não desvie. Revisite esta lista periodicamente
> (início de ciclo, sessão exploratória).
>
> Isto **não** é o registro de decisões — decisão fechada com dado vai para o §9
> do `SPEC1.md`. Aqui fica dívida em aberto.

| Data | Achado | Onde | Prioridade | Estado |
|---|---|---|---|---|
| 28/08/2026 | Fixtures do Moodle nunca passaram pela higienização do §3.3 — têm `userid`, nome e nota. Ficam gitignoradas até lá. | `fixtures/moodle/raw/` | alta (bloqueia versionar fixture de teste) | ✅ fechado 31/08 — `scripts/higienizar.py` existe e três fixtures do Moodle estão versionadas higienizadas (`action_events`, `course_contents_psi3323`, `users_courses`). O `raw/` segue gitignorado, e é o certo |
| 31/08/2026 | HTML de turma nomeia professor real e traz sala/vagas; gitignorado como evidência, pendente de higienização. | `fixtures/jupiter/html-obterTurma-*.html` | média | 🔵 aberto |
| 31/08/2026 | Não existia gate automatizado — o `CLAUDE.md` §3 e o `CONVENTIONS.md` §4 tinham `<TODO>`. | repo | média | ✅ fechado 31/08 — `scripts/gate.sh`: segredo, cru ignorado, suíte offline; verificado por sabotagem das três checagens; decisão no §9 |
| 31/08/2026 | `server.py` lia `MOODLE_TOKEN`/`MOODLE_URL` de `os.environ` sem ninguém carregar o `.env` para ele. | `usp_mcp/moodle/server.py` | média | ✅ fechado 31/08 — `usp_mcp/env.py` compartilhado entre `conftest` e `server`; decisão no §9 |
| 31/08/2026 | Não existia fixture de erro do Moodle: os testes de erro asseguravam o contrato da camada, nunca a forma do erro real. | `fixtures/moodle/`, `tests/moodle/test_cliente.py` | média | ✅ fechado 31/08 — três fixtures reais (`invalidtoken`, `invalidparameter`, `limite_fora_da_faixa`) ligadas em T52/T56/T57. HTML de manutenção e timeout não são capturáveis sob demanda e seguem como contrato de camada |
| 31/08/2026 | Três caminhos sem teste offline: o transporte HTTP real do cliente, o caminho autenticado de `chamar_ferramenta` e o adaptador stdio de `main()`. Verde na suíte não é verde neles. | `usp_mcp/moodle/{cliente,server}.py` | média | 🟡 parcial 31/08 — o `main()` fechou por dois caminhos (T78-T81 em processo e `tests/handshake/` no fio) e o caminho autenticado de `chamar_ferramenta` fechou por injeção de cliente (T80), **sem credencial**. Segue aberto só o transporte HTTP real — e ele já custou o bug do timeout de 15 s |
| 31/08/2026 | O `.venv` não existe no repo nem em worktree novo, e o `.env` também não (gitignorado, não copiado por `git worktree add`). | repo | baixa | ✅ fechado 31/08 — `requirements-dev.txt` criado; o `.env` passou a ser achado pelo `conftest`, o venv segue por diretório (é o certo) |
| 31/08/2026 | `main()` do servidor MCP foi escrito contra a API antiga do SDK (`Server` + `@list_tools`) e não funcionava com o `mcp` 2.1.1 (`MCPServer` + `@tool`). A suíte estava 99/99 verde com ele quebrado — nenhum teste o alcança. | `usp_mcp/moodle/server.py` | alta | ✅ fechado 31/08 — reescrito e verificado por handshake stdio real; `--auto-verificar` reduz a chance de repetir |
| 31/08/2026 | §1.1 afirmava como fato que a rede da USP não é alcançável do sandbox; verdadeiro para o Cowork em nuvem, falso para o Claude Code na máquina do dono. Foi repetido em decisões sem ser remedido. | `SPEC1.md` §1.1, `CLAUDE.md` item 9 | alta | ✅ fechado 31/08 — ambos reescritos com a medição; decisão no §9 |
| 31/08/2026 | `o_que_vence` não mandava `limitnum` e o default do Moodle é 20: perdia entregas silenciosamente e reportava `truncado=False`. A suíte não pegava porque o duplo de cliente devolve o que o teste manda — nenhuma asserção olhava o parâmetro ENVIADO. | `usp_mcp/moodle/o_que_vence.py` | alta | ✅ fechado 31/08 — manda `limitnum=50` e declara quando bate no teto; T53/T54/T55; decisão no §9 |
| 31/08/2026 | O adaptador stdio `main()` do servidor do Jupiter não tem teste: a suíte cobre as funções puras, e verde nelas não é verde nele. Mesmo buraco que a trilha do Moodle registrou — e lá ele escondeu um `main()` escrito contra a API antiga do SDK. `--auto-verificar` reduz, não elimina. | `usp_mcp/jupiter/server.py` | alta | ✅ fechado 31/08 — por dois testes que não se substituem: T45-T48 rodam `main()` em processo (corpo enviado, erro de SDK ausente) e `tests/handshake/` sobe o processo e confere o fio. Decisões no §9 |
| 31/08/2026 | O §9 e o handoff dizem que a fronteira do Moodle não é testável ponta a ponta **porque falta um token**. A causa é outra: `chamar_ferramenta` do Moodle não aceita `cliente` injetável, e o do Jupiter aceita. Com a injeção, o teste roda offline com dublê e sem credencial nenhuma. Medido: 3 dos 4 testes T45-T48 já rodam no `main()` do Moodle hoje, sem token — registrou `o_que_vence`, transporte stdio, schema x assinatura OK. | `usp_mcp/moodle/server.py`, `SPEC1.md` §9 | alta | ✅ fechado 12/09 — **estava obsoleto, não foi trabalho**: `chamar_ferramenta` aceita `cliente` injetável desde o T80 (`usp_mcp/moodle/server.py:169`, `*, cliente=None`), e a linha 17 desta tabela já registrava isso. A linha passou doze dias marcada *alta* sem haver dívida |
| 31/08/2026 | Comentário dentro de `chamar_ferramenta` ainda afirma que o caminho autenticado "fala com a rede da USP, que o sandbox não alcança (§1.1)" — o §1.1 foi corrigido em 31/08 e a rede É alcançável da máquina do dono. Resíduo do erro já registrado, agora em código em vez de documento. | `usp_mcp/moodle/server.py` | média | ✅ fechado 12/09 — **estava obsoleto**: o comentário não existe mais em `server.py` (`grep` por `sandbox`, `§1.1` e `rede da USP` não acha o texto; as duas ocorrências que sobraram são do `--auto-verificar`, e dizem o contrário — que nada ali toca a rede) |
| 31/08/2026 | O adaptador stdio `main()` do servidor do Jupiter não tem teste: a suíte cobre as funções puras, e verde nelas não é verde nele. Mesmo buraco que a trilha do Moodle registrou — e lá ele escondeu um `main()` escrito contra a API antiga do SDK. `--auto-verificar` reduz, não elimina. | `usp_mcp/jupiter/server.py` | alta | ✅ fechado 31/08 — T45-T48 rodam `main()` contra o SDK real, substituindo só `run()`; cada asserção verificada por sabotagem, e uma delas revelou T47 fraco (ver §9). Verde com e sem o SDK instalado |
| 31/08/2026 | O §9 e o handoff dizem que a fronteira do Moodle não é testável ponta a ponta **porque falta um token**. A causa é outra: `chamar_ferramenta` do Moodle não aceita `cliente` injetável, e o do Jupiter aceita. Com a injeção, o teste roda offline com dublê e sem credencial nenhuma. Medido: 3 dos 4 testes T45-T48 já rodam no `main()` do Moodle hoje, sem token — registrou `o_que_vence`, transporte stdio, schema x assinatura OK. | `usp_mcp/moodle/server.py`, `SPEC1.md` §9 | alta | ✅ fechado 31/08 — `chamar_ferramenta` aceita `cliente` injetável; T78-T81 rodam o `main()` contra o SDK real sem credencial, incluindo `call_tool` ponta a ponta |
| 31/08/2026 | Comentário dentro de `chamar_ferramenta` ainda afirma que o caminho autenticado "fala com a rede da USP, que o sandbox não alcança (§1.1)" — o §1.1 foi corrigido em 31/08 e a rede É alcançável da máquina do dono. Resíduo do erro já registrado, agora em código em vez de documento. | `usp_mcp/moodle/server.py` | média | ✅ fechado 31/08 — o comentário saiu junto com a refatoração que introduziu a injeção |
| 31/08/2026 | Registrei aqui que a questão do iCal do §4 "nunca foi medida", inferindo isso de `fixtures/ical/` conter só um `.gitkeep`. Ela está **fechada com dado desde 31/08**, no §9: 37 eventos em 18 kB contra 35 em 541 kB, e a decisão de não usar o iCal como fonte (segundo segredo, ICS sem `courseid` utilizável) está registrada. Inferi de uma ausência de fixture em vez de ler o registro que estava logo ali — e cheguei a recomendar como prioridade uma medição já feita. Terceira vez no mesmo dia que uma afirmação sai de memória/inferência em vez de um comando barato. | processo, `docs/decisions/BACKLOG-correcoes.md` | média | ✅ fechado 31/08 — linha corrigida na mesma sessão; a lição é a do item 9 do `CLAUDE.md` aplicada a registro, não só a rede: **ausência de arquivo não é ausência de fato** |
| 31/08/2026 | O download de arquivo do Moodle com o token anexado à `fileurl` nunca foi verificado — é o padrão do Moodle e por isso a tentação é assumir. Bloqueia a segunda ferramenta de material (ler o PDF). Custa uma chamada da conta: decisão do dono. | `notas/fase1-moodle.md`, §9 | média | ✅ fechado 01/09 — funciona, e custou **zero** chamada de web service (as `fileurl` da fixture higienizada são reais). Dois achados: o token **não precisa ir na URL** (corpo do POST serve, premissa contrária estava escrita no §9 e na docstring), e erro de credencial vem como **HTTP 200** com JSON. Medição dos 19 PDFs no §9 e em `notas/fase1-moodle.md` |
| 31/08/2026 | Não se sabe se `core_course_get_contents` expande o conteúdo de um `mod_folder`. PSI3323 tem zero pastas, então a amostra da Fase 1 não responde — e uma disciplina que agrupe as listas numa Pasta é justamente o caso que a ferramenta de material precisa acertar. | `usp_mcp/moodle/`, §4 | média | 🟢 fechado em 15/09/2026 — `core_course_get_contents` **expande sim**. `mod_folder_get_folders_by_courses` com as doze matrículas de 2026 achou dez pastas, todas em `PEA3301_2026_1sem`, e as outras onze disciplinas não têm nenhuma, o que explica a amostra vazia de 31/08. As dez chegam com `contents` preenchido, e 48 dos 89 arquivos da disciplina (54%) moram dentro de pasta. `material` já mostra todos: ele itera o `contents` de todo módulo e não só dos `resource`. Medição em `notas/mod-folder.md` |
| 31/08/2026 | O transporte HTTP real segue sem teste, e isso já custou um bug: o timeout de 15 s estourava na `get_users_courses` (14,7 s medidos) com a suíte 201/201 verde. T82 trava o piso do timeout, mas é guarda de constante, não teste do transporte. | `usp_mcp/moodle/cliente.py` | média | 🔵 aberto |
| 31/08/2026 | Resolver disciplina por nome não casa abreviação: `lab de eletronica` não acha `Laboratório de Eletrônica I`. Acento já foi corrigido (NFD, T83); abreviação é casamento aproximado, decisão de escopo e não bug. | `usp_mcp/moodle/disciplinas.py` | baixa | 🔵 aberto |
| 31/08/2026 | A projeção de 11,2%, as 16 seções e a mistura de tipos da ferramenta `material` vêm de **uma amostra só** (PSI3323), por decisão do dono. Uma disciplina com resumo de seção longo pode ter razão bem pior, e o teto do T67 foi posto generoso por causa disso. Quem for tratar como fato do sistema precisa capturar outras antes. | `usp_mcp/moodle/material.py`, `tests/moodle/test_material.py` | média | 🔵 aberto |
| 31/08/2026 | A higienização embaralha `fullname`, então resolver disciplina por **nome** não é exercitável contra a fixture versionada — T62 testa a regra contra lista sintética. O caminho por nome funciona em produção e não tem regressão de dado real. | `tests/moodle/test_material.py`, `scripts/higienizar.py` | baixa | 🔵 aberto |
| 31/08/2026 | Não há fixture pareada de disciplina + pré-requisito da MESMA disciplina, então T31 cobre só a composição de duas chamadas. O pareamento foi confirmado ao vivo (MAT2454 → MAT2453), mas ao vivo não é regressão. | `fixtures/jupiter/`, `tests/jupiter/test_disciplina.py` | média | 🔵 aberto |
| 31/08/2026 | O significado de `verdis` continua não verificado: a Fase 1 passou `0` e recebeu `verdis:"2"`. O cliente manda `0` porque é o que o JS oficial manda, sem saber se isso é "corrente" ou coincidência. | `usp_mcp/jupiter/cliente.py` | baixa | 🔵 aberto |
| 31/08/2026 | O `CLAUDE.md` §1 e o `README.md` afirmavam que a Fase 2 não tinha começado e que não havia servidor MCP, com dois rodando. Duraram dois dias sem ninguém notar — estado em documento falha calado. | `CLAUDE.md`, `README.md` | alta | ✅ fechado 31/08 — estado saiu do `CLAUDE.md` (guarda regra, não notícia); `README.md` reescrito; decisão no §9 |
| 31/08/2026 | Eu afirmei em três documentos que a rede da USP não era alcançável desta sessão, com a regra 9 do `CLAUDE.md` ("antes de dizer que a rede não dá, MEÇA") em contexto e com este backlog já registrando o mesmo erro como fechado. Custou um `curl` desmentir. Sinal de que aviso escrito não substitui verificação barata executada. | processo | média | ✅ fechado 31/08 — canários rodados, três documentos corrigidos, lição no §9 |
| 31/08/2026 | O adaptador stdio `main()` do RUCard não tem teste — **terceira trilha com o mesmo buraco** (Moodle e Jupiter já o registraram, e no Moodle ele escondeu um `main()` escrito contra a API antiga do SDK). Aqui foi verificado à mão por handshake stdio real, o que prova hoje e não protege amanhã. Três repetições sugerem que a cura é um teste de handshake compartilhado, não mais uma linha de backlog. | `usp_mcp/*/server.py`, `tests/` | alta | ✅ fechado 31/08 — `tests/handshake/` sobe o processo de cada servidor descoberto em `usp_mcp/*/server.py` e aperta a mão com ele; verificado por sabotagem de três tipos de defeito. Decisão no §9 |
| 31/08/2026 | Não há fixture de `menu` com HTML nem com separador ` - `, que o §1.2 registra como existentes. Duas semanas de captura (2 × 7 dias × 2 refeições × 4 RUs) não produziram nenhuma. O parser tolera os dois e o teste do parser é de comportamento, não de forma — mas a tolerância nunca viu o caso real. | `fixtures/rucard/`, `usp_mcp/rucard/ferramentas.py` | média | 🔵 aberto |
| 31/08/2026 | A situação `fechado` (horário publicado + cardápio fechado, isto é, feriado) é a única da ferramenta do RUCard testada com entrada **fabricada**: nenhum feriado caiu nas duas semanas capturadas. Uma captura em semana de feriado fecharia isso com dado real. | `tests/rucard/test_bandejao.py` | média | 🔵 aberto |
| 31/08/2026 | `docs/domains/README.md` afirmava "hoje não há código" com três pacotes em `usp_mcp/`. **Duas sessões registraram o mesmo achado de estado desatualizado no mesmo dia, sem se ver** (a linha acima, sobre `CLAUDE.md` e `README.md`) — o sinal não é o documento, é que estado escrito em muitos lugares envelhece calado em todos. | `docs/domains/README.md` | média | ✅ fechado 31/08 — reescrito; o `CLAUDE.md` deixou de guardar estado por decisão do §9, e o `README.md` passou a ser o resumo com dono |
| 31/08/2026 | O SDK do MCP deriva o schema que o modelo vê da ASSINATURA da função, não do `inputSchema` declarado — então nenhuma descrição de parâmetro dos três servidores e nenhum `enum` do RUCard chegava ao modelo. Só apareceu quando o handshake passou a ser testado; `--auto-verificar` não pegava, porque comparava declaração com assinatura e nunca com o fio. | `usp_mcp/*/server.py` | alta | ✅ fechado 31/08 — `usp_mcp/adaptador.py` prende a descrição declarada à anotação; H6–H8 verificam no fio. Decisão no §9 |
| 31/08/2026 | Com o `enum` chegando ao fio, argumento inválido passa a ser barrado pela validação do SDK **antes** da nossa política — o que é bom (nenhuma requisição sai), mas a mensagem é a do pydantic, em inglês: "Input should be '6', '7', '8' or '9'". A nossa negativa em português, que explica que o RU existe e está fora de escopo, só aparece por outros caminhos de chamada. Invariante 6 atendido pela metade. | `usp_mcp/*/server.py` | baixa | 🔵 aberto |
| 01/09/2026 | `pypdf` seria a **primeira dependência de runtime** do projeto — hoje só `mcp`. Foi instalado para medir o texto dos PDFs e desinstalado depois, de propósito: adotá-lo é decisão de §9, não efeito colateral de uma medida. Sem ele não há segunda ferramenta de material. | `requirements.txt`, §9 | média | ✅ fechado 01/09 por **descarte**: a ferramenta entrega o caminho do arquivo em disco e quem lê é o agente que chamou, então não há extração no servidor e nenhuma dependência nova. Decisão no §9 |
| 01/09/2026 | O acervo tem `.docx`, `image/jpeg` e `application/octet-stream` além dos 19 PDFs, e a medição de texto cobriu só PDF. A segunda ferramenta precisa dizer o que faz com os outros — recusar com motivo é resposta legítima (Invariante 6), sumir com eles não é. | `usp_mcp/moodle/material.py` | baixa | 🔵 aberto — mudou de natureza com a decisão do §9 de 01/09: entregando o caminho, o servidor não precisa entender o formato, e o limite passa a ser o que o **cliente** sabe abrir. A ferramenta ainda tem de dizer o que baixou |
| 01/09/2026 | O parâmetro `busca` de `material` filtra com `filtro in nome.lower()`, **sem** a normalização NFD que `disciplinas._normalizar` faz. Medido: `"formulario"` não acha `"Formulário Provas Substitutivas.pdf"`. É o mesmo bug do acento corrigido em 31/08 (T83), sobrevivendo em outro lugar do mesmo módulo — a correção de lá tratou a resolução de disciplina e não a busca por arquivo. | `usp_mcp/moodle/material.py` | média | ✅ fechado 02/09 — `usp_mcp/moodle/texto.py` passou a ser o único lugar do casamento (`normalizar` + `casa`), servindo `disciplinas`, `material` e `arquivo`. T102 trava o acento na busca |
| 02/09/2026 | A docstring de `tests/moodle/test_server_mcp.py` afirma "Só três testes, e é intencional" — o arquivo já tinha 5 antes desta sessão (T100/T101 chegaram com `baixar_arquivo`) e ganhou mais 1 nesta revisão (T106). Achado ao adicionar teste de descrição no finding 4 da revisão final de `baixar_arquivo`; não corrigido para não desviar da tarefa. | `tests/moodle/test_server_mcp.py` | baixa | 🔵 aberto |
| 02/09/2026 | `cliente.baixar` levanta `ErroMoodle` **cru** em dois casos — o teto de bytes pós-download (`cliente.py:257`) e o tamanho divergente do `filesize` (`cliente.py:262`). Como `_motivo_seguro` mapeia por tipo e o default é genérico por decisão de segurança, os dois chegam ao modelo como "falha ao baixar este arquivo", perdendo a distinção. Só alcançável quando o item não declara `filesize` ou o servidor trunca. A cura é dar a cada um sua subclasse de `ErroMoodle`. | `usp_mcp/moodle/{cliente,arquivo}.py` | baixa | 🔵 aberto |
| 03/09/2026 | O depósito de arquivos (`~/.cache/usp-mcp/moodle/`) **só cresce**: não há limpeza por idade nem teto agregado de disco. Cada versão nova de um arquivo (`timemodified` diferente) ganha diretório próprio e o antigo fica. Apagar a árvore à mão é seguro por construção, mas isso é dívida, não desenho. | `usp_mcp/moodle/deposito.py` | baixa | 🔵 aberto — **cura identificada em 03/09** pela pesquisa de `notas/mcpb-e-distribuicao.md`: limpeza **no startup**, como o `mcp-clip` faz. Ela desfaz a objeção original (sumir com arquivo em uso), porque no startup nada está sendo lido. O TTL não se copia de lá: 1 hora serve a dado efêmero, e material de disciplina muda por semestre (Invariante 5) |
| 03/09/2026 | O §9 de 01/09 registrou "zero escaneados: 19 de 19 têm camada de texto" a partir de PSI3323, e a primeira disciplina nova refutou: `Lista 1.pdf` e `Lista 2.pdf` de PTC3314 são manuscritos escaneados, **0 B de texto por página**. A afirmação era verdadeira para a amostra e falsa como fato do sistema. Corrigida no §9 de 03/09. | `SPEC1.md` §9, `notas/fase1-moodle.md` | média | ✅ fechado 03/09 — §9 registra o caso e a nota ganhou a ressalva no lugar onde a leitura fácil enganaria |
| 03/09/2026 | `baixar_arquivo` casa o termo **só contra o nome do arquivo** (`arquivo.py:159`), ignorando `secao` e `modulo` — campos que o `Item` já carrega e que a própria resposta de ambiguidade imprime. Os nomes de arquivo do e-Disciplinas são opacos: em PTC3314, `Lista 2.pdf` mora no módulo "Resolução Exercícios do Capítulo 3 da apostila do curso", então "resolução do capítulo 3" não acha o arquivo que É a resolução do capítulo 3. Verificado ao vivo em 03/09. O fluxo `material` → escolher pelo nome exato contorna, ao custo de uma ida e volta. **Não é conserto de uma linha:** casar contra módulo e seção multiplica a ambiguidade ("aula" casaria com quase tudo em PTC3314), então pede desempate por prioridade — nome primeiro, módulo/seção só quando o nome não achar nada — e isso é decisão de desenho, não ajuste. | `usp_mcp/moodle/arquivo.py` | média | ✅ fechado 03/09 — e o diagnóstico da linha estava um nível abaixo do problema: a informação era descartada antes, na projeção que `material` imprime. A cura que ficou: `material` emite o rótulo do módulo quando ele acrescenta (20 de 29 itens; os 9 redundantes são omitidos), e o modelo escolhe na lista. Uma segunda metade — casamento por palavras no servidor — chegou a ser construída e verificada ao vivo, e foi **revertida no mesmo dia**: busca semântica é do modelo, não de heurística de string (§9, 03/09) |
| 03/09/2026 | Empacotar o Moodle como `.mcpb` nunca foi tentado, e o §6.1 registra o formato só por leitura de documentação. Duas coisas dependem de uma instalação real numa máquina limpa e **não** de estimativa: quanto atrito o runtime Python devolve (o `.mcpb` recomenda Node porque ele vem com o Claude Desktop), e **onde** o `"sensitive": true` do `user_config` guarda o valor — a spec do manifest diz "armazena com segurança" e não diz onde, e o Invariante 3 quer saber. | `notas/mcpb-e-distribuicao.md`, §6.1 | média | 🔵 aberto |
| 03/09/2026 | O Claude Desktop roda em macOS e Windows, não Linux. Se o `.mcpb` virar o caminho de distribuição, quem usa Linux fica com o `git clone` + venv de hoje — e não há nada escrito para essa pessoa. | `README.md` | baixa | 🔵 aberto |
| 10/09/2026 | `scripts/ws.sh` passa o token em `argv` do `curl` (`--data-urlencode "wstoken=$MOODLE_TOKEN"`), e `ps aux` é legível por qualquer processo do mesmo usuário — a credencial fica publicada na máquina enquanto a chamada vive. Achado ao escrever o `token.sh`, que **não** tem o furo: medido em 10/09 que `curl -K -` lê a opção do stdin e o campo chega igual no corpo do POST. A cura é de duas linhas, mas o `ws.sh` é o script que toda a descoberta usa e `scripts/{capture,userid}.sh` chamam ele — mexer nele é mexer no chão. Fora de escopo da tarefa do token. | `scripts/ws.sh` | média | ✅ fechado 15/09 — e **saiu tudo do argv, não só o token**: `wsfunction`, `moodlewsrestformat` e os parâmetros do usuário também vão por `curl -K -`, porque duas rotas para montar o mesmo corpo são dois lugares para errar o escape e uma regra que ninguém confere de fora. Com uma rota só a invariante é mecânica — nenhum `--data-urlencode` no argv — e é essa que T1 varre, por diretório. Achado no caminho: o arquivo de configuração do `curl` desfaz `\\`, `\"`, `\t`, `\n`, `\r` e `\v` dentro de aspas, então parâmetro arbitrário exige escape ou a chamada sai silenciosamente errada; T5 confere o corpo POST contra o `curl` de verdade. Decisão no §9 |
| 10/09/2026 | A conferência do passaporte do `token.sh` (passo 5) usa `md5(wwwroot + passport)` para o `siteid`, e essa fórmula está **recordada, não medida** contra o e-Disciplinas — por isso ela avisa em vez de bloquear (§1.4). Fecha com uma execução real: se o passo 6 autenticar e a conferência tiver batido, a fórmula está confirmada e a checagem pode virar bloqueio, que é o que detecta payload colado de outra tentativa. Custa **zero** chamada extra — o diagnóstico sai da mesma rodada de setup. | `scripts/token.sh`, §1.3 | baixa | 🔵 aberto |
| 10/09/2026 | Os passos 3 e 4 do `token.sh` (abrir navegador, ler clipboard) não são alcançáveis pela suíte offline, e o §7 do desenho diz isso em voz alta em vez de fingir cobertura. O resto do script foi exercitado contra um Moodle de mentira em `127.0.0.1`, à mão, nesta sessão — o que prova hoje e não protege amanhã. É o mesmo padrão que o handshake stdio fechou para os servidores; a cura análoga seria uma camada que sobe o servidor falso e roda o script. | `scripts/token.sh`, `tests/` | baixa | 🟡 parcial 14/09 — o caminho `pbpaste \| ./scripts/token.sh` roda na suíte (T-tok-19 a T-tok-22): sem tty o script não abre navegador nem tenta a captura automática, e com um `curl` dublado no PATH ele vai até gravar o `.env`. Isso alcança **o texto que o passo 3 imprime** e **a conferência sobre o valor que chega**. Continua fora de alcance o que a linha diz: abrir o navegador, e ler um clipboard de verdade — o valor entra por stdin, e ninguém testou que `pbpaste` é chamado |
| 10/09/2026 | `forcedurlscheme` (linha 111 do `launch.php`) sobrescreve o `urlscheme` que o `token.sh` manda, e se o e-Disciplinas tiver isso configurado a captura automática nunca dispara — cai no manual, com a mensagem certa, mas sem que ninguém saiba se é isso ou outra coisa. Não é observável de fora: só uma rodada real com token responde. Custo zero, sai da mesma execução de setup. | `scripts/_capturar_redirect.sh`, §1.3 | média | 🔵 aberto |
| 10/09/2026 | A captura automática e o fallback dela para o caminho manual **não têm teste na suíte**, e não devem ter: registrar handler no Launch Services e abrir navegador mudariam a máquina de quem roda o gate. A verificação é medição à mão contra um Moodle de mentira, registrada no §9 de 10/09 — prova hoje e não protege amanhã. Mesmo padrão que o handshake stdio fechou para os servidores; a cura análoga seria uma camada opcional atrás de env var, como a `live`. | `scripts/_capturar_redirect.sh`, `tests/` | média | 🔵 aberto |
| 10/09/2026 | `_capturar_redirect.sh` usa caminho fixo (`~/Library/Caches/usp-mcp-token`) e faz `rm -rf` nele ao começar: duas execuções simultâneas se atropelam, e a segunda apaga o FIFO da primeira. É script de setup rodado uma vez, então o risco é teórico — mas o `trap` não cobre `kill -9`, e aí sobra handler registrado até alguém rodar de novo. A mensagem de limpeza já ensina a remover à mão. | `scripts/_capturar_redirect.sh` | baixa | 🔵 aberto |
| 11/09/2026 | `scripts/ws.sh` sourceia só `./.env` (linha 15) e um worktree não tem o dele — lá ele falha com "MOODLE_TOKEN vazio, copie .env.example", que é a cura errada, porque o `.env` existe no checkout principal e o lado Python o acha por `achar_env`. Menos grave que o defeito irmão do `token.sh` corrigido em 11/09 (ali o risco era CRIAR um `.env` que sombreia o verdadeiro); aqui o erro é legível, só aponta para o lugar errado. Mesma cura: perguntar ao `usp_mcp.env`. | `scripts/ws.sh`, `scripts/capture.sh`, `scripts/userid.sh` | média | 🔵 aberto |
| 11/09/2026 | Três verificadores meus seguidos casaram string demais em vez de assertar a condição: a limpeza do handler casava a mensagem de erro do `open` (reportou "ainda atende" numa máquina limpa), a guarda de esquema contava claims sem olhar o identificador (recusou por causa de registro obsoleto do próprio handler), e o polling do teste casava "handler **pronto**" no lugar do "pronto" final. Nenhum é bug de produto, os três são do mesmo molde e custaram três diagnósticos errados numa sessão. O §6 do `CONVENTIONS.md` já diz "não afrouxe uma verificação"; falta dizer o corolário — **verificação casa a condição, não a mensagem**. | processo, `docs/agents/CONVENTIONS.md` | média | 🔵 aberto |
| 11/09/2026 | Medido em 11/09 com payload falso e servidor local: o **Chrome não entrega** `uspmcp://` ao Launch Services sem um clique — abre diálogo próprio de confirmação e espera. O navegador padrão do sistema (Safari nesta máquina) entrega **em segundos e sem perguntar**, nos dois testes locais. Então `--navegador="Google Chrome"` existe como escape, mas a recomendação é o padrão. Não medido: se o diálogo do Chrome tem "sempre permitir", o que tornaria a segunda execução silenciosa — importa para quem não usa Safari. | `scripts/_capturar_redirect.sh`, `README.md` | baixa | 🔵 aberto |
| 11/09/2026 | A conferência do passaporte do `token.sh` não distingue "payload de outra rodada minha, válido" de "payload que não é desta conta" — as duas dão `siteid` diferente. No caminho manual ela avisa quase sempre, porque nada obriga a pessoa a abrir a URL que o script imprimiu (aconteceu na rodada real de 11/09: avisou, e o token autenticou). A mensagem já diz isso; a **checagem** continua sem poder de discriminação fora do caminho automático. Cura possível: no manual, só conferir se a pessoa disser que abriu a URL impressa — ou aceitar que ali ela não vale e não imprimir nada. | `usp_mcp/token/cli.py` (era `scripts/token.sh`, portado em 18/09) | baixa | 🔵 aberto |
| 10/09/2026 | Toda mensagem de erro dos três servidores morria na fronteira MCP: o modelo recebia `Error executing tool <nome>` (29-32 bytes) e o texto em português ficava só no stderr. Regressão do SDK, isolada por A/B no mesmo commit — o `Tool.run` do `mcp 2.0.0` tinha um ramo só e repassava o texto; o do 2.2.0 separa `ToolError`/`ResourceError` (a mensagem viaja) de `except Exception` (crash, e "the exception's own text stays on the server"). `ErroRucard`/`ErroJupiter`/`ErroMoodle` herdam de `Exception`, então caíam no ramo de crash, e `requirements.txt` declara `mcp>=2,<3`: toda instalação nova nascia muda. Invariante 6 quebrado no único lugar onde o modelo lê. A suíte ficava verde porque T45-T48/T78-T81 e o handshake exercitam o caminho feliz e o `tools/list`, e nenhum olhava o texto de erro no fio. | `usp_mcp/*/server.py` | alta | ✅ fechado 10/09 — a tradução mora na fronteira, no adaptador stdio: só `ErroX` do próprio domínio vira `ToolError(str(exc)) from exc`, e `ErroX` segue sendo a moeda interna. Nada de `except Exception` — crash continua com o texto retido, que é o comportamento certo do SDK (e o que impede o traceback de 46 frames do Tomcat de voltar ao modelo). Bateria E1-E4 por servidor (`tests/*/test_erro_no_fio.py`, 12 testes) escrita ANTES da correção e verificada vermelha pelo motivo certo; a correção verificada por sabotagem no Jupiter e no Moodle |
| 10/09/2026 | `resolver_dia` aceita `%d/%m` (data sem ano, "26/08") de propósito e corrige o ano em seguida com `.replace(year=hoje.year)`. O Python 3.14 já emite `DeprecationWarning` nesse `strptime`, e o 3.15 vai "sempre levantar exceção ou usar outro ano padrão (a definir)" — se levantar, a forma sem ano some sem que nada no fonte diga por quê, e a pessoa que digitar "26/08" recebe "não entendi o dia". Apareceu como o único warning da suíte quando E1 passou a exercitar o caminho de recusa; é código de produção, não do teste. A cura provável é montar a data à mão em vez de delegar o ano ao `strptime`, e é decisão de §9 (a forma sem ano continua valendo? com que ano, na virada do ano?), não ajuste. | `usp_mcp/rucard/ferramentas.py` | média | 🔵 aberto |
| 10/09/2026 | 12 testes (T3 ×4, R3 ×8) reprovavam em qualquer checkout sob pasta acentuada, acusando ".gitignore" — falso. O diretório está em **NFD** no disco do macOS e o `pathlib` entrega **NFC**; o git compara byte a byte, não acha o prefixo do repo e devolve `rc=128 ... is outside repository` para arquivo ignorado E versionado. O `assert r.returncode == 1` lia esse 128 como resposta de negócio. Dois defeitos somados: caminho absoluto onde relativo bastava, e `rc` de erro lido como "sim". Mesma família do acento fechado em 31/08 (T83) e 02/09 (`texto.py`), agora na suíte em vez do produto. | `tests/{jupiter,rucard}/test_fixtures.py` | alta (o gate reprovava commit por motivo errado num clone limpo) | ✅ fechado 10/09 — `tests/git.py::esta_ignorado` é o único lugar que pergunta ao git: relativo à raiz (imune a NFD/NFC) e `rc` fora de `{0,1}` vira `RuntimeError` citando o rc. G1-G5 em `tests/test_git.py`; G3 monta repo real sob `ação` em NFD e G5 varre `tests/` para impedir a volta. Verificado sabotando **cada propriedade em separado**: só o absoluto reprova os 12 + G1/G2/G3, só o rc sem guarda reprova G4. Achado no caminho: com o helper devolvendo `bool`, T3/R3 sozinhos ficam **cegos** ao rc=128 (128→`False`→"não ignorado"→verde) — quem os mantém honestos é a guarda de `rc`, não a asserção deles. `tests/moodle/test_higienizacao.py` (T47) entrou junto: já era relativo e correto, mas um segundo lugar certo é de onde o próximo call site copia |
| 10/09/2026 | A guarda da Regra de Ouro (§3.1) apodreceu calada por dez dias: T51 congelava a `ALLOWLIST` em **um** nome e ela foi a quatro em 31/08, por decisão registrada no §9. Dois defeitos, e o segundo é o que importa — a asserção congelou um **valor** em vez da propriedade, e morava na camada `live`, que o `gate.sh` exclui de propósito (um gate que depende da USP estar de pé reprova commit por motivo errado). Guarda atrás de `USP_MCP_LIVE=1` não roda em commit nenhum: mesmo modo de falha que este backlog já registrou três vezes para o `main()`. | `tests/moodle/test_live.py`, `tests/moodle/test_politica.py` | média (a guarda em si é alta — protege a credencial do dono) | ✅ fechado 10/09 — T51 sai da camada `live` e vira P1-P5 em `test_politica.py`, onde o gate as roda: todo nome chamado pela live está na `ALLOWLIST` (P1), nenhum nome do §2.2 aparece no fonte dela (P2), a live não itera sobre a lista (P3, por AST), P1-P3 não carregam a marca `live` (P4 — é o teste que impede a repetição) e toda função da allowlist casa com um prefixo de leitura conhecido (P5 — teto que cresce sem congelar). P1 e P5 verificados por sabotagem |
| 10/09/2026 | Num clone limpo, seguindo o `README.md` de cima para baixo, o primeiro comando que alguém novo roda reprovava. O `./scripts/gate.sh` estava na seção *Rodando* e o `cp .env.example .env` só na *Configuração*, depois — então o gate rodava sem `.env`. Nenhuma das duas mensagens nomeava o passo que faltou: a checagem 1 dizia "sem .env em lugar nenhum" e a 3 falava de `RUCARD_HASH` ausente, depois de 364 testes. Afrouxar o `test_r8` seria trocar um vermelho honesto por um verde que não verificou nada (Invariante 6); o defeito era de ordem, no papel e no script. | `README.md`, `scripts/gate.sh` | média | ✅ fechado 10/09 — o `cp` subiu para antes do gate com a razão escrita ao lado, e o gate ganhou a **checagem 0** (`.env` existe e tem `RUCARD_HASH` com valor), que aborta citando o comando exato e **não** exige `MOODLE_TOKEN`, porque o gate é offline e credencial pessoal não é pré-requisito para commitar (Invariante 4). Bateria D1-D5 em `tests/test_documentacao.py` e `tests/test_gate.py`, escrita vermelha antes da cura; verificada por sabotagem e por clone limpo ponta a ponta |
| 10/09/2026 | O `.mcp.json` versionado chamava `.venv/bin/python` por caminho **relativo**, o que transfere ao cliente a premissa de rodar o servidor com o cwd na raiz do projeto. O Claude Code faz isso e o bug ficou invisível; qualquer cliente que não faça — o Claude Desktop entre eles, alvo do `.mcpb` do §6.1 — recebia rc=127 e `no such file or directory`. Medido: `cd /tmp && .venv/bin/python -m usp_mcp.rucard.server`. As duas saídas óbvias estão as duas erradas: absoluto no arquivo versionado é caminho de máquina no git (Invariante 3 aplicado a caminho), relativo é falha calada. | `.mcp.json`, `scripts/servidor.sh`, `README.md` | baixa hoje, alta no dia do `.mcpb` | ✅ fechado 10/09 — `scripts/servidor.sh` resolve a própria raiz e sobe de qualquer cwd; o `.mcp.json` segue relativo apontando para ele, e o único absoluto passa a ser o do script, no config da máquina de quem usa (seção nova no `README`, com `<CAMINHO-DO-CHECKOUT>`). L1-L6 em `tests/test_lancador.py`; verificado à mão subindo os três de `/tmp` e por sabotagem do `.venv`. **Não reescreve o §6.1**: o `.mcpb` continua não testado e ganha agora um entrypoint com nome — quem for empacotar decide se o `manifest.json` chama o lançador ou o interpretador com `${__dirname}`, e isso é decisão de §9, não efeito colateral desta correção |
| 11/09/2026 | **Terceira vez que o mesmo defeito aparece:** procurar o `.env` só no diretório atual. Já mordeu a checagem de segredos do gate (§4 do `CONVENTIONS.md`), o `token.sh`/`fix-token.sh` (corrigidos hoje) e agora a checagem 0 do gate, que chegou na `main` em 11/09 e **reprovava em todo worktree** — no mesmo arquivo em que as checagens 1 e 3 já perguntavam ao `usp_mcp.env.achar_env`. Corrigida aqui porque bloqueava o merge. Três repetições sugerem que a cura não é mais uma correção: é `achar_env` ser a única porta, e nenhum `[ -f .env ]` sobreviver em script nenhum. `scripts/ws.sh` ainda tem o seu. | `scripts/`, `docs/agents/CONVENTIONS.md` | alta | ✅ fechado 12/09 — `ws.sh` pergunta ao `achar_env`, e **nenhum `[ -f .env ]` sobrou em `scripts/`** (o de `token.sh:97` é `.env.example`, outra coisa). A regra virou frase no §4 do `CONVENTIONS.md`, porque a cura da terceira repetição não é mais uma correção. W1/W2 travam os dois lados |
| 12/09/2026 | `scripts/ws.sh` e `scripts/capture.sh` procuram o `.env` só no diretório atual (`[ -f .env ]`), então **não funcionam em worktree**: o `.env` mora no checkout principal e é gitignorado. O lado Python já resolve isso (`usp_mcp/env.py` sobe até o `.git` de verdade); o lado bash não. Custou uma sonda escrita em Python só para medir uma chamada. | `scripts/ws.sh`, `scripts/capture.sh` | média | ✅ fechado 12/09 pelo mesmo commit — e **a premissa sobre o `capture.sh` estava errada**: ele não tem `[ -f .env ]` próprio, chama `./scripts/ws.sh`, e o mesmo vale para o `userid.sh`. Consertar o chão consertou os três |
| 12/09/2026 | O `intro` do assign — o enunciado em TEXTO — segue fora de alcance, por decisão de escopo do dono (12/09): `material` é lista de arquivos. **Medido nas 23 entregas do semestre, numa chamada:** 19 têm `intro` vazio, 2 têm só as datas (79 e 193 ch, e `o_que_vence` já dá isso), e **2 têm conteúdo real** — `Projeto Disciplina` (274 ch: entregar slides + documento por grupo) e `E1 - 1ª Parte` de PSI3472 (248 ch: alocação de aeronaves). A segunda é o caso que dói: **ela não tem anexo nenhum**, então o enunciado dela existe só aí, e hoje o rodapé a declara como "sem arquivo anexado", o que soa como "o professor não postou nada". Custo de incluir: zero chamada nova (o campo já vem na resposta que `acervo` busca), ~130 tokens de saída no semestre inteiro. | `usp_mcp/moodle/material.py`, §5 | média | 🔵 aberto |
| 12/09/2026 | `introfiles` nunca foi medido: na captura de PTC3314 ele vem vazio nas 4 entregas, e `acervo` lê só `introattachments`. Se alguma disciplina usar o outro campo, o arquivo some da lista sem aviso — que é exatamente o bug que esta sessão fechou, em outro campo. | `usp_mcp/moodle/material.py` | baixa | 🔵 aberto |
| 12/09/2026 | A suíte cobria `material` com uma fixture que TEM um módulo `assign` e nunca perguntou o que havia dentro dele — o bug dos anexos passou por 428 testes verdes. Vale procurar o mesmo padrão nas outras ferramentas: onde a suíte confirma o caminho que existe sem nunca perguntar "esta resposta é tudo?". | `tests/` | média | 🔵 aberto |
| 12/09/2026 | `baixar_arquivo` casa por nome de ARQUIVO (decisão de 03/09), e o nome do enunciado de entrega é opaco: `EP1-2026.pdf` é o "EC-1", `PSI3481_exercicio_com_nota_2024_IP3.pdf` é o "exercício de cascata de amplificadores". O assunto está no nome do MÓDULO, que `material` já imprime e o filtro não olha — quem pedir pelo nome da atividade paga uma ida a mais. Não é o casamento semântico que foi medido e revertido em 03/09; é estender o substring a um campo já exibido. | `usp_mcp/moodle/arquivo.py`, `usp_mcp/moodle/material.py` | média | 🔵 aberto |
| 12/09/2026 | O rodapé de `material` anunciava resto negativo: `if sobra := len(mudas) - _TETO_NOMES_NO_RODAPE:` testa "diferente de zero", e abaixo do teto a subtração dá negativo, que é truthy. Saiu ao vivo em PTC3314 com as 2 entregas mudas já nomeadas e um `, e mais -1` no fim. O T122 mediu só o caso ACIMA do teto (11 de 11 em PSI3472), e o defeito morava exatamente do outro lado — teste de teto escrito por um lado só. Não é cosmético: quem lê o rodapé é um modelo decidindo se já viu tudo, e um resto inventado o manda procurar entrega que a ferramenta não tem, que é o Invariante 7 ao contrário. | `usp_mcp/moodle/material.py` | média | ✅ fechado 12/09 — `> 0` explícito e T123 parametrizado em torno do teto (1 a 5); verificado por sabotagem e contra a conta real |
| 13/09/2026 | `resolver` nunca casa contra o `rotulo` (`shortname`), só contra `sigla` e `fullname`. Na USP é invisível porque sigla é o 1º segmento do rótulo; em Moodle com shortname fora do padrão "SIGLA-ano" a disciplina existe e não é encontrada. | `usp_mcp/moodle/disciplinas.py` | média | ✅ fechado 15/09 — **e o defeito era maior do que esta linha dizia.** Medido nas duas capturas desta conta: o rótulo inteiro, que é o que a ferramenta `disciplinas` imprime na tela, não resolvia em 69 de 74 (44 de 47 na de 15/09). E o irmão dele, a sigla vinda de `split("-")[0]`, não é só portabilidade: o separador do `shortname` desta conta é `-` (69), `_` (2), espaço (1), `.` (1) e nenhum (1), e `PEA3301_2026_1sem` — semestre corrente — ganhava sigla própria enquanto `PEA3301-2021` ficava com `PEA3301` sozinha. Ao vivo em 15/09, `material` com "PEA3301" respondeu 177 itens sobre a de 2021, sem ambiguidade nenhuma para detectar. A sigla passa a ser o primeiro pedaço alfanumérico, o rótulo entra na busca (exato antes de parcial, nos dois níveis), e o empate vira pergunta com as candidatas ordenadas pela vigência. DI16-DI23 |
| 13/09/2026 | `slasharguments=0` no Moodle faz a `fileurl` sair como `pluginfile.php?file=/…`; `_PREFIXO_ARQUIVO` e `_fileid` exigem a barra, e o arquivo vira **link externo com URL vazia** — falha muda, contra o Invariante 6. | `usp_mcp/moodle/cliente.py`, `material.py` | média | 🔵 aberto — `notas/portabilidade-moodle.md` §4.3 |
| 13/09/2026 | `MOODLE_URL` com barra no fim monta `//webservice/pluginfile.php/` e faz a checagem de origem do `baixar` recusar todo download. Falta normalizar o valor na construção do cliente. | `usp_mcp/moodle/cliente.py` | baixa | ✅ fechado 16/09 — `ClienteMoodle.__init__` normaliza com `rstrip("/")`; T63 em `test_cliente.py` prova função e download com a barra |
| 14/09/2026 | A auditoria que fechou o furo do §2.2 classificou as 57 candidatas a escrita **contra as categorias existentes**, e parou aí. Duas classes ficaram olhadas mas não decididas: as funções de **nota e prazo** (`mod_assign_save_grade*`, `mod_diary_save_feedback`, `mod_journal_save_feedback`, `mod_assign_save_user_extensions`) foram deixadas fora porque são capacidade de quem corrige e a conta de aluno não as alcança — mas "o servidor negaria" não é garantia nossa, e o dia em que o projeto servir conta de monitor ou PAE elas viram alcançáveis sem que nada aqui mude. E `mod_simplecertificate_create_issue` só está fora porque a conclusão que a antecede foi bloqueada agora — se a conclusão sair da lista, esta fica órfã. As duas estão registradas em `ESCRITAS_FORA_DE_ESCOPO` com motivo, então a decisão existe; o que não existe é o gatilho que a revisita. | `usp_mcp/moodle/politica.py`, §2.2 | média | 🔵 aberto |
| 14/09/2026 | A varredura por verbo de escrita é regex sobre nome de função (`_add`, `_save`, `_submit`, `_process`, `_send`, `_create`, `_start`, `_launch`, `_enrol`…), e nome não é contrato: o §2.2 já registra que `core_course_set_favourite_courses` **se declara `read` e grava**. Uma função de escrita batizada fora desse vocabulário não apareceu nas 57 e ninguém a olhou. A varredura fecha o que ela alcança, não o conjunto. | `tests/moodle/test_politica.py` | baixa | 🔵 aberto |
| 14/09/2026 | A ferramenta `diagnostico` responde "o que este token alcança" e o `scripts/compatibilidade.sh` responde "este site tem o serviço ligado", sem credencial. As duas são complementares e **nada no código liga uma à outra além de prosa**: a docstring e a descrição citam o script, mas nenhum teste garante que a citação sobreviva a um rename. Se o script mudar de nome, a ferramenta passa a apontar para um caminho que não existe, e quem estiver sem token fica sem o passo anterior. | `usp_mcp/moodle/diagnostico.py`, `scripts/compatibilidade.sh` | baixa | 🔵 aberto |
| 14/09/2026 | `PTC3313` devolve 26.623 B e **zero** currículo, e a explicação escrita na saída ("da ênfase e do módulo em diante esse endpoint costuma não ter registro") é hipótese de UMA amostra. Se for falso, a ferramenta está desculpando um silêncio que tem outra causa. | `usp_mcp/jupiter/ferramentas.py` | média | 🔵 aberto |
| 14/09/2026 | O mapeamento rótulo↔`tipreq`/`stamtrrcp` tem **3 pontos** (MAT2455 em 3250 e 3032, PSI3323 em 3032). Um quarto rótulo sai como `desconhecido` — o que é o comportamento certo, mas ninguém sabe se ele existe. | `usp_mcp/jupiter/requisitos.py` | baixa | 🔵 aberto |
| 14/09/2026 | `disciplina` com `codcur` que não carrega requisito diz "pode não haver exigência **ou** a disciplina não pertence a esse currículo" — falta a terceira causa, agora medida: o código pode ser de outra geração do mesmo currículo (3033 × 3032). A mensagem devia mandar para `requisitos`. | `usp_mcp/jupiter/ferramentas.py` | média | 🔵 aberto |
| 14/09/2026 | A fatia `curso` (navegação unidade→curso, `pubGradeCurricular`) saiu do escopo com a medição de 14/09 e continua aberta: ela destrava **grade curricular**, não pré-requisito. As duas consultas de navegação já estão na allowlist. | `usp_mcp/jupiter/` | baixa | 🔵 aberto |
| 14/09/2026 | Como o app móvel do RUCard autentica usuário na camada `/servicos` — a única pista de auth pessoal que não passa por sessão de navegador. Exige ler o binário do app; o JS do site web não menciona a camada. Medido em `notas/fase1-rucard.md`. | `notas/fase1-rucard.md` | baixa | 🔵 aberto |
| 14/09/2026 | A área logada do JupiterWeb (nota, histórico, evolução do curso) tem plano de reconhecimento escrito e **nada executado**: falta a Fase 0 (script de sessão, `.env.example`, checagem no gate), que não toca a rede e pode ser feita sem a credencial. O plano vive em `docs/superpowers/plans/2026-09-14-jupiter-sessao-recon.md`. | `scripts/`, `usp_mcp/env.py` | média | 🔵 aberto |
| 14/09/2026 | A fatia `curso` sai do backlog como **não será construída** para "o que falta pra formar": medido que a grade curricular dos cursos de ingresso da Poli para no 5º semestre (4 cursos medidos), e o dono está no 7º+. Se voltar, volta por outra pergunta registrada no §5. | `SPEC1.md` §9 | — | ✅ fechado 14/09 — por medição, sem implementação |
| 14/09/2026 | A política decide sobre um nome que o fio **não é obrigado a honrar**: em `cliente.chamar`, `dados = {..., "wsfunction": funcao, **params}` põe `params` por último, então uma chave `wsfunction` ali vence a que a allowlist aprovou. Medido com transporte falso (nenhuma requisição saiu): a política aprovou `core_course_get_contents` e o fio recebeu `mod_assign_submit_for_grading` — que é negada pelo nome no §2.2 e nem `USP_MCP_ALLOW_WRITES=1` libera. **Não alcançável hoje**: os seis `cliente.chamar(...)` do pacote usam chaves literais e nenhuma ferramenta repassa `**kwargs` do modelo. Cura: recusar `wstoken`/`wsfunction`/`moodlewsrestformat` em `params`, e um teste que asserte o que SAIU no fio — o T29 confere o `wstoken` enviado e ninguém confere o `wsfunction` (item 11 do `CLAUDE.md`). | `usp_mcp/moodle/cliente.py:171` | alta | ✅ fechado 16/09 — `_requisitar` recusa `wstoken`/`wsfunction`/`moodlewsrestformat` em `params` com `FuncaoBloqueada` antes do transporte, e o envelope entra por último; T61 asserta o `wsfunction` que SAIU no fio |
| 14/09/2026 | `filesize` ausente apaga **quatro guardas de uma vez, caladas**: o teto por arquivo (`arquivo.py:228`, `if item.tamanho and ...` — `None` não entra), o orçamento agregado do `todos` (`arquivo.py:246`, `item.tamanho or 0` soma zero), a conferência pós-download (`tamanho_esperado=None`) e a integridade do reuso (`deposito.ja_baixado` cai para "existe e não está vazio"). No fim, `Baixado(tamanho=item.tamanho or caminho.stat().st_size)` reporta o tamanho do que sobrou com cara de certo. Cada função degrada bem sozinha; nenhuma **diz** que degradou (Invariante 7). **Não observado**: medido que os 9 conteúdos sem `filesize` nas duas fixturas (7 em PSI3323, 2 em PTC3314) são todos `type: url`, que viram `Link` antes do download — mesmo estado do `mod_folder`, não observado ≠ inexistente. | `usp_mcp/moodle/{arquivo,cliente,deposito}.py` | média | 🔵 aberto |
| 14/09/2026 | O buraco do transporte real (item 31, escrito só para o Moodle) é **igual nos três clientes**, medido: só `test_cliente`, `test_arquivo` e `test_bandejao` injetam transporte, e o Jupiter injeta por posição. `_transporte_padrao`, `_transporte_download_padrao`, o do RUCard e as 216 linhas do `dwr.py` só executam na camada `live`, que é opt-in do dono. Já custou o bug do timeout de 15 s com a suíte verde. | `usp_mcp/*/cliente.py`, `usp_mcp/jupiter/dwr.py` | média | 🔵 aberto |
| 14/09/2026 | O `.mcpb` (§6.1) segue **não testado**, e o `pyproject.toml` de hoje é o degrau ANTERIOR a ele, não o substituto: pacote com entry point mata o `git clone` + venv, e não responde as duas perguntas que travam o bundle — quanto atrito o runtime Python devolve numa máquina limpa, e ONDE o `"sensitive": true` do `user_config` guarda o valor (o Invariante 3 quer saber). Nenhuma das duas sai de documentação; as duas exigem instalação real no Claude Desktop, que roda em macOS e Windows e não em Linux. | `SPEC1.md` §6.1, `notas/mcpb-e-distribuicao.md` | média | 🔵 aberto |
| 14/09/2026 | O `notas/moodle-catalogo.md` marca como **[notas]** (isto é, "medido na Fase 1") o custo de três funções que a `notas/fase1-moodle.md` lista como **não medidas**, na seção "Ainda aberto": `mod_assign_get_submission_status` (~260 tokens por assign) e as duas visões de nota (~870 e ~320 tokens). Achado ao procurar um número de partida para a projeção de `ja_entreguei`: os três são plausíveis e **não têm fonte**, que é exatamente o modo de falha que o §9 de 27/08 registra como "número plausível sem fonte é o jeito mais fácil de um documento de descoberta virar documento de viés". Fecha com uma captura ao vivo das três, ou rebaixando a marca para **[inferência]** no catálogo. | `notas/moodle-catalogo.md` §3.5 e §3.7, `notas/fase1-moodle.md` | média | 🔵 aberto |
| 14/09/2026 | As fixtures de `mod_assign_get_submission_status` e das duas `gradereport_*` que a suíte exercita são **escritas à mão**, a partir da forma documentada do core 5.0, e não capturadas. A execução ao vivo de 14/09 mostrou que uma delas divergia da realidade: a resposta de `gradereport_overview_get_course_grades` tem três campos por item, e a inventada tinha `rank`, `maxrank` e `averageformatted`, que não existem. O código não dependia deles, então nada quebrou — mas a suíte está afirmando forma contra um dicionário que ela mesma inventou, que é exatamente o que o `test_anexos_de_entrega.py` diz não fazer. A cura é capturar as três, higienizar (§3.3) e trocar. | `tests/moodle/conftest.py`, `fixtures/moodle/` | média | 🔵 aberto |
| 14/09/2026 | As duas respostas de fórum que a suíte de `avisos` exercita (`mod_forum_get_forums_by_courses` e `mod_forum_get_forum_discussions`) são **escritas à mão**, como as de nota e de entrega da linha acima, e por isso a razão de projeção de 2,8× do §9 mede payload inventado. Aqui a dívida é maior que nas outras duas por três motivos: a ferramenta economiza chamada com base em `numdiscussions`, que ninguém viu chegar; a ordenação depende de o fórum *Avisos* ter `type: "news"`; e `perpage`/`page` vão explícitos contra defaults não medidos. A captura também é a mais cara de higienizar — é o payload com mais nome de terceiro do projeto (21% dos bytes de uma discussão), e o §3.3 se aplica a todos eles. | `tests/moodle/conftest.py`, `fixtures/moodle/` | média | 🔵 aberto |
| 14/09/2026 | A resposta de `core_course_get_updates_since` que a suíte de `o_que_mudou` exercita é **escrita à mão**; só os `cmid` são reais (de `course_contents_ptc3314.json`). A dívida aqui é menor que a de fórum e de nota — o `get_contents` que domina a projeção é fixture real, e 99,4% do cru medido vem dele —, mas três afirmações seguem sem confirmação e as três são baratas: quais `name` de mudança o e-Disciplinas usa de fato, se `contextlevel` vem `module` e `course` como o core declara, e se `since` no futuro devolve vazio em vez de erro (é a hipótese por trás de recusar `dias <= 0`). Fecha com uma chamada, na mesma execução que capturar a fixture. | `tests/moodle/conftest.py`, `fixtures/moodle/` | baixa | 🔵 aberto |
| 14/09/2026 | A sigla sai de `shortname.split("-")[0]`, e a ferramenta `disciplinas` tornou visível que o `shortname` real do e-Disciplinas tem pelo menos três formatos que esse corte erra: separador por espaço (`PSI3211 2025` → sigla `PSI32112025`), por ponto (`2166.2023i`) e código sem sigla nenhuma (`0300071-150-2026`, `AEX-IF-00020.01`). Nenhum quebra hoje — `resolver` casa por começo de sigla e por pedaço do nome, então `PSI3211` ainda acha `PSI3211 2025` —, mas o efeito é uma sigla estranha impressa na lista e uma ambiguidade a mais quando duas matrículas da mesma matéria coexistem. Fecha medindo os formatos de `shortname` da conta ao vivo e decidindo se o corte vira regex. | `usp_mcp/moodle/disciplinas.py`, `fixtures/moodle/users_courses.json` | baixa | 🔵 aberto |
| 14/09/2026 | `users_courses.json` (capturada em 31/08, higienizada) tem **74 matrículas**, e a execução ao vivo de 14/09 contou **45**. A divergência não foi explicada e nenhuma das duas é obviamente a errada — pode ser filtro diferente na captura, pode ser matrícula que saiu da conta. Importa porque as duas aparecem como "medido" em lugares diferentes do §9, e porque é a fixture que sustenta a projeção de `disciplinas` e a resolução de sigla de todas as ferramentas com escopo. Fecha recapturando e comparando o conjunto de `id`. | `fixtures/moodle/users_courses.json`, `SPEC1.md` §9 | média | ✅ fechado 14/09 — não era bug: a fixture de 31/08 está velha (40 saíram, 13 entraram), os `id` dela são reais (não são higienizados), e o `gradereport` cobre um conjunto menor que o `enrol`. Registrado no §9 |
| 14/09/2026 | `ja_entreguei` **engole os `warnings`** de `mod_assign_get_assignments`, e a fixture real de PTC3314 traz dois ("sem direito de acesso" a dois módulos). `material` os mostra desde 12/09 e `atrasadas` passou a mostrá-los hoje; a de `ja_entreguei` é a mesma resposta lida pela mesma projeção, e lá eles somem — o que faz a lista de entregas dela parecer completa quando o próprio Moodle avisou que não é (Invariante 7). Fecha acrescentando o aviso à saída, com o teste que o trava. | `usp_mcp/moodle/ja_entreguei.py` | média | ✅ fechado 15/09 — e os dois avisos saem **separados por origem**, não somados: o da lista põe em dúvida a EXISTÊNCIA da entrega, o do status põe em dúvida o VEREDITO de uma que apareceu, e um número só apagaria qual é qual (J23 trava o somado ausente). A unidade do aviso de status é a ENTREGA e não o item de warning. O aviso da lista entra nos três ramos, inclusive nos dois vazios — `courses` vazio COM warning é "não te deixei ver" e não "não há o que entregar" (J24). Conferido que `core_enrol_get_users_courses` não tem o campo: é lista crua, sem envelope. J21-J25; decisão no §9 |
| 15/09/2026 | `notas` lê `grademax` para preencher o "máximo" de cada item, e **o campo não chega deste site**: a captura de 15/09 tem 20 itens e nenhum o traz (F2 trava). O renderizador degrada sozinho — `de {maximo}` só sai se houver valor —, então a saída nunca mentiu; o que existe é um campo morto e uma promessa vazia no dataclass. Decidir: tirar `maximo` de vez, ou derivar de `percentageformatted` + `gradeformatted` (que é inferência, não dado). | `usp_mcp/moodle/notas.py` | baixa | ✅ fechado 15/09 — **tirado de vez**, e não derivado: a captura tem 26 chaves distintas por item e nenhuma carrega o máximo, nem com outro nome (`graderaw`, `gradeformatted` e `percentageformatted` dizem quanto se tirou, não de quanto era), então derivar seria apresentar inferência arredondada como dado (Invariante 6). Saiu o campo do dataclass, a leitura, o ramo do renderizador, o aviso de "peso e máximo" e a promessa na descrição da ferramenta que o modelo lê. F8/F9 em `tests/moodle/test_forma_real.py` travam a volta: F1-F6 já garantiam que o dublê não INVENTA campo, e agora a regra espelhada garante que a projeção não LÊ um |
| 15/09/2026 | `fixtures/moodle/course_contents_ptc3314.json`, versionada desde antes desta sessão, carrega o nome de um professor **68 vezes**: 18 em `name` de módulo ("Slides Aula 01 - Prof. Trinti"), 50 dentro de `customdata`, e 1 num `fileurl` para a página pessoal dele. Isso é material publicado, e não identidade de conta — mas o projeto **gitignora** `fixtures/jupiter/html-obterTurma-*.html` exatamente por "nomear professor real". As duas regras não podem valer ao mesmo tempo, e a decisão fica mais cara com o repositório indo a público. Higienizar o `name` quebraria o rótulo de módulo, que é o que a correção de 03/09 existe para entregar. | `fixtures/moodle/`, §3.3 | média | 🔵 aberto — 16/09: o caso concreto saiu pela rede de honorífico (linha abaixo); a pergunta de política (material publicado que nomeia professor: gitignorar como o HTML do Jupiter, ou higienizar como agora?) continua do dono |
| 15/09/2026 | O nome do professor saiu da `course_contents_ptc3314.json`, mas ele foi trocado por um sintético **à mão**, não pelo `higienizar.py`: nome de pessoa dentro de `name` de módulo ("Slides Aula 01 - Prof. X") é texto livre, e o higienizador não tem como saber que aquele pedaço é gente. A próxima captura de uma disciplina cujo professor assine o material vai trazer o nome de volta, e ninguém será avisado. Cura possível: `CAMPOS_TEXTO_LIVRE` não cobre `name` de propósito (o rótulo do módulo é o produto), então ou se aceita a passagem manual e se escreve isso no §3.3, ou se inventa uma checagem que compare a captura nova contra uma lista de nomes conhecidos — que exigiria guardar a lista, e ela é o dado que não queremos guardar. | `scripts/higienizar.py`, §3.3 | média | ✅ fechado 16/09 — rede de honorífico em `higienizar.py`: `Prof.`/`Profa.`/`Dr.`/`Dra.` seguido de nome, em QUALQUER string, vira nome sintético do mesmo tamanho (T55). Os 18 `name` de PTC3314 e o `shortname` de PME3100 passaram por ela, e `alertas()` acusa o que voltar. Nome **sem** honorífico em texto livre continua passando, e a varredura não tem como acusá-lo — limitação declarada na docstring do script |
| 15/09/2026 | `tests/test_git.py::test_g3` só passa em sistema de arquivos insensível a normalização. Ele cria uma pasta acentuada em NFD e pergunta ao git sobre a MESMA pasta escrita em NFC — no APFS do Mac são a mesma entrada (e é lá que o defeito que ele reproduz acontece); no ext4 são dois nomes, o segundo não existe, e o teste morre com `FileNotFoundError` no `cwd` antes de medir o que veio medir. Medido em 15/09 no primeiro CI que existiu (rodada 35013389922, Linux): **735 de 736**, nas duas versões do Python. A cura é o teste PULAR declarando o motivo quando o sistema de arquivos separa as duas formas — a mesma forma barulhenta que o resto da suíte usa —, e não afrouxar a asserção. Enquanto isso o CI roda em macOS, que é onde o projeto é desenvolvido e usado; fechar isto libera o runner Linux, que custa 10x menos minuto num repositório privado. | `tests/test_git.py`, `.github/workflows/offline.yml` | média | 🟢 fechado em 16/09/2026 — o `test_g3` passou a checar se o sistema de arquivos separa NFD de NFC e pula declarando o motivo quando separa; o CI foi para `ubuntu-latest` e o C8 passou a exigir Linux. O flanco que sobra está escrito no texto do pulo: o BUG-2 só é exercitado em sistema insensível a normalização, pelo gate local |
| 16/09/2026 | A entrada de 01/09 do §9 documenta a limpeza dos conectores da conta **citando o que limpou**: uma tabela com onze conectores claude.ai, o UUID de cada um, o hostname de um servidor MCP interno, o nome da empresa e o id do plugin privado dela. É o mesmo defeito que tirou o `.claude/settings.json` do git em 16/09 (desligar nomeando é publicar a lista), só que no documento que é a autoridade do projeto. Achado ao varrer o repositório para a abertura ao público, e deixado em aberto de propósito: o §9 é registro histórico datado, reescrevê-lo é decisão do dono e não cabia na trilha que mexia em configuração. Fecha decidindo entre resumir a entrada sem os identificadores ou mantê-la e aceitar o que ela expõe. | `SPEC1.md` §9, entrada de 01/09/2026 | **alta (bloqueia tornar o repositório público)** | 🟢 fechado em 16/09/2026 — a entrada do §9 foi reescrita: a decisão, o número de ferramentas e o que foi descartado ficaram; a tabela com os onze UUIDs, o hostname interno e os nomes de skill de terceiro saíram. Nenhum teste nem script dependia do trecho |
| 16/09/2026 | A decisão do **marcador da página de requisitos do Jupiter** foi tomada com dado medido e ainda **não entrou no §9 do `SPEC1.md`** — esta sessão não podia tocar naquele arquivo. O marcador é a conjunção de `<title>Jupiterweb</title>` e da palavra `equisito`, as duas sem diferenciar maiúscula; a medição está no comentário de `usp_mcp/jupiter/cliente.py` e nos testes T94-T97. O que precisa ficar registrado é o descartado: a sigla não serve (a fixtura de zero currículo não contém a própria), `Curso:` não serve (é o que separa "tem currículo" de "não tem", e exigi-lo viraria o mesmo defeito do avesso), e o título sozinho não serve (as seis outras páginas capturadas o têm). | `SPEC1.md` §9 | média | 🔵 aberto |
| 16/09/2026 | **Limite declarado do marcador acima:** das seis outras páginas HTML do JupiterWeb capturadas, a conjunção rejeita cinco; a sexta é `html-obterDisciplina-PSI3323.html`, que passa só porque carrega um LINK para `listarCursosRequisitos`. Não é alcançável hoje — este caminho nunca pede aquela URL — mas se a fatia crescer para mais de um caminho HTML, a conjunção deixa de discriminar entre eles e o marcador precisa virar específico por caminho. | `usp_mcp/jupiter/cliente.py` | baixa | 🔵 aberto |
| 16/09/2026 | O aviso de "nenhum currículo com exigência" (`_SEM_CURRICULO`) continua listando **duas** causas, e agora isso é verdade: a terceira ("a página que chegou não era a de requisitos") deixou de ser alcançável porque o cliente recusa a página antes de recortar. A dívida que fica é a que o backlog de 14/09 já registrava por outro motivo: a explicação sobre ênfase e módulo é hipótese de UMA amostra (PTC3313). Se ela for falsa, o aviso segue desculpando um silêncio com a causa errada — só que agora com as duas causas certas em número. | `usp_mcp/jupiter/ferramentas.py` | média | 🔵 aberto |
| 16/09/2026 | A janela da **falha lembrada** do RUCard (`TTL_FALHA = 5 min`) sai de uma conta, não de medição: ela precisa cobrir uma varredura de semana com os quatro RUs fora do ar (4 × 20 s de tempo limite antes de o segundo dia começar). Nunca foi medida contra a USP de verdade — não se sabe com que frequência o `/menu` de um RU falha nem por quanto tempo. Se falhar em rajadas curtas, 5 minutos é longo demais e cala o RU depois de ele voltar; se falhar por horas, é curto e a economia é menor do que parece. | `usp_mcp/rucard/cliente.py` | baixa | 🔵 aberto |
| 16/09/2026 | Revisão do higienizador de fixtures do Moodle: decisão por nome exato de campo dava passagem a dado pessoal pontual em fixtures já publicadas. Corrigido por família (prefixo/sufixo) e conteúdo, com varredura sobre toda publicada (T56) e CLI que recusa gravar o que ainda acusa (§9, 16/09). Achados pontuais tratados como risco aceito. O revisitar que esta linha prometia aconteceu em 17/09 e fechou: o histórico fica como está, e abrir o repositório não depende de reescrevê-lo (§9, 17/09/2026, com a medição que sustentou a decisão). | `scripts/higienizar.py`, `fixtures/moodle/` | — | ✅ fechado 16/09 |
| 17/09/2026 | O CI não roda por **cobrança do GitHub Actions**, e não por defeito deste repositório: com o workflow já carregando, os dois jobs da matriz são criados e recusados na largada com *"The job was not started because recent account payments have failed or your spending limit needs to be increased"*. **Já era sabido em 16/09** — a mensagem está citada na mensagem do commit `7ecf2e4`, e foi ela que motivou a troca do runner de macOS por Linux, que custa um décimo do minuto. O que faltava era registro fora do histórico do git: nenhum documento deste repositório citava a cobrança, e o YAML quebrado no mesmo commit apagou a única pista visível — sem job criado, a run passou a falhar em 0s e a mensagem deixou de aparecer. Enquanto isso, nenhuma PR tem checagem automática: o `gate.sh` local volta a ser a única porta, com o defeito de origem (só roda quando alguém lembra). Nota lateral: para repositório **público** o Actions em runner padrão não consome a cota paga — a decisão de abrir o repositório muda esta linha. | conta do GitHub, `.github/workflows/` | alta (o CI existe e não roda) | ✅ fechado 17/09 — a própria linha previa a saída, e foi ela: **repositório público não consome a cota paga**. Medido no mesmo dia, depois de abrir: seis runs criadas, seis completas, `gate (3.11)` e `gate (3.14)` em 42–54s cada, nas duas PRs e nos dois merges. O CI voltou a ser porta de PR, e o `gate.sh` local deixou de ser a única |
| 17/09/2026 | O `gate.sh` reprova **na máquina do dono** em T58 (`o_cru_quando_existe_reproduz_a_publicada`), nos pares `grades_overview.json` e `users_courses.json → users_courses_15-09.json`. Não é regressão do higienizador: o cru que o `_achar_dir_cru()` alcança no checkout principal é a captura de **31/08**, e a publicada de `users_courses` é a de **15/09** — o teste compara duas capturas diferentes e acusa o que é diferença de dado, não de higienização. Fica invisível no CI, onde `raw/` não existe e T58 pula, então o único lugar em que ele aparece é justamente o gate que barra commit. O canário está cego para o que foi escrito para vigiar. Cura: ou recapturar o cru dos dois pares e reconferir, ou tirar o par cuja publicada veio de outra data da `PARES_CRU` dizendo por quê. | `tests/moodle/test_higienizacao.py`, `fixtures/moodle/raw/` | média (o gate reprova commit por motivo alheio à mudança em curso) | ✅ fechado 17/09 — e era **pior** do que esta linha dizia. Dos sete pares declarados, cinco nomeavam cru que não existe no disco: T58 pulava calado neles e reprovava nos dois restantes, que eram de outra captura. Verificação real: zero, nas duas pontas. Remedido por varredura exaustiva offline (14 crus × 15 publicadas): três reproduzem byte a byte e são os únicos que ficam, com T58b travando o número. Recaptura das nove de 15/09 segue sendo decisão do dono, porque custa chamada da conta |
| 17/09/2026 | O User-Agent que os dois clientes mandam para a USP se anuncia como `usp-mcp/0.1`, e o pacote está em `1.0.0` desde 15/09. Não quebra nada — a USP não lê esse número —, mas é a única coisa que este projeto diz sobre si mesmo do lado de lá, e ela está desatualizada em nove meses de trabalho. A versão está declarada num lugar só (`pyproject.toml`) e copiada à mão em dois; a cura é ler de `importlib.metadata.version()` em vez de repetir o literal. Achado ao trocar a URL do agente no rename de 17/09. | `usp_mcp/jupiter/cliente.py`, `usp_mcp/rucard/cliente.py` | baixa | ✅ fechado 17/09 — `VERSAO` e `AGENTE` passam a viver em `usp_mcp/__init__.py`, com a versão perguntada a `importlib.metadata` em vez de repetida; os dois clientes importam o mesmo valor. P7 trava o agente contra o `pyproject.toml` e recusa o valor de fora-de-instalação, para não medir o vazio |
| 17/09/2026 | `notas/moodle-catalogo.md` afirma o projeto **mais aberto** do que ele está, em dois pontos da família `core_message_*`. O §4.1 diz "24 escritas, das quais o §2.2 bloqueia **uma**" e marca `core_message_send_messages_to_conversation` como "não bloqueado"; a tabela por função, no fim do documento, marca só `send_instant_messages` como `bloqueada-§2.2`. E o §6.4 lista `send_messages_to_conversation` e `create_contact_request` entre os nomes que "faltam" ao §2.2. As duas entraram: a primeira está no `BLOQUEIO_PERMANENTE` desde 31/08/2026 (commit `892f6b1`, mesmo dia do catálogo), a segunda desde a auditoria de 14/09. Hoje são **três** bloqueadas na família, e o catálogo diz uma. Achado de passagem enquanto se levantava o spec de mensagem (17/09). Documentação que erra para o lado permissivo é a pior direção do erro: quem consultar o catálogo em vez do código conclui que há caminho de envio aberto. | `notas/moodle-catalogo.md` §4.1, §6.4 e a tabela `core_message_*` | média | 🔵 aberto |
| 18/09/2026 | No Linux, `command -v open` acha o `openvt` do pacote `kbd` (Debian e Ubuntu instalam `/usr/bin/open` como alternativa dele), e o `token.sh` o escolhia antes do `xdg-open`: chamaria `openvt URL`, que falha, e o script diria "falhou ao abrir" e mandaria abrir à mão. Degrada com aviso, não em silêncio. Achado ao desenhar o Windows, porque o Ubuntu do WSL tem esse `open`; lá o `wslview` passou a vir antes e cobre o caso. Num Linux sem WSL a cura é preferir `xdg-open` quando `uname -s` não é `Darwin`. **Não medido em Linux real** — inferido do pacote `kbd`. | `usp_mcp/token/cli.py` (era `scripts/token.sh`, portado em 18/09) | baixa | 🔵 aberto |
| 18/09/2026 | **O intervalo da vigia é 0,5 s para todo leitor, inclusive o PowerShell**, e no PowerShell cada leitura sobe um processo: 180 arranques numa janela de 90 s. Um intervalo maior foi escrito e **descartado antes de entrar** (§9, 18/09) por falta de medição: "200 a 800 ms de arranque" é estimativa, e mudar o padrão da vigia com base nela é o que o projeto não faz. O que medir, e só numa máquina Windows de verdade: quanto custa de fato um `powershell -NoProfile -Command Get-Clipboard`, e se a vigia com 0,5 s chega a durar mais do que os 90 s que ela anuncia (Invariante 7). Se durar, o intervalo por leitor volta — e aí com número. Atenção ao efeito colateral já observado: um intervalo maior que a janela dá zero tiques e a vigia deixa de ler, o que exige piso de um tique ou encolher o intervalo até caber. | `usp_mcp/token/cli.py` (era `scripts/token.sh`, portado em 18/09) | baixa | 🔵 aberto |
| 18/09/2026 | Uma correção nascida de argumento geral foi aplicada a **um** lugar só: o §4 do `2026-09-18-windows-design.md` pôs `wslview` na frente de `open` porque "sob WSLg, `xdg-open` abriria um navegador Linux", e o mesmo `$DISPLAY` fazia o `xclip` vencer na escolha do **leitor** de clipboard — corrigido em 18/09, no mesmo dia. Vale varrer se há outra escolha do repositório que dependa de `$DISPLAY`/`$WAYLAND_DISPLAY` e herde a mesma premissa; `scripts/_capturar_redirect.sh` é o candidato óbvio e não foi olhado. | `scripts/_capturar_redirect.sh` | baixa | 🔵 aberto |
| 21/09/2026 | **O `.mcp.json` versionado atrapalha no Windows, e a decisão do que fazer com ele é do dono.** Relatado por quem rodou o projeto num Windows real em 21/09/2026: quem abre um cliente MCP dentro do clone recebe (i) aviso de que `usp-rucard` e `usp-jupiter` estão definidos em dois escopos com endpoints diferentes, o de usuário (que funciona) e o de projeto (que não), e (ii) `usp-moodle` de escopo de projeto pendente de aprovação, e ele nunca vai funcionar ali. A causa é a cadeia `.mcp.json` → `scripts/servidor.sh` (bash) → `.venv/bin/python` (caminho POSIX), que o Windows não tem. O relato conclui certo: as entradas de escopo de projeto não funcionariam lá de qualquer jeito. **O que o spec de 18/09 já decidiu e continua valendo:** os nove scripts de `scripts/` seguem POSIX de propósito, porque são de quem mantém e ninguém que só usa os roda. O `.mcp.json` é a exceção que aquele spec não olhou — ninguém o roda, o cliente o recolhe sozinho pelo fato de a pasta estar aberta. **E não é só Windows:** o caminho de instalação do README clona em `~/usp-mcp` e registra em escopo de usuário, então qualquer pessoa que depois abra um cliente dentro do clone tem os dois escopos ao mesmo tempo; no Mac e no Linux os dois funcionam e o custo é só o aviso, no Windows um dos dois é morto. A precedência documentada do Claude Code é Local > Projeto > Usuário, o que faria a entrada morta vencer a que funciona dentro daquela pasta — **documentado, não medido aqui, e não medível sem um Windows**. **Quem depende do arquivo hoje:** L3 e L4 (`tests/test_lancador.py`) leem o arquivo e são os únicos testes que quebrariam se ele sumisse; L1, L2, L5 e L6 exercitam o `servidor.sh` direto e sobrevivem. Nenhum script o lê, e o `gate.sh` não o toca. O resto é prosa que cita o arquivo e precisaria de emenda: `CLAUDE.md` §3, cabeçalho do `pyproject.toml`, cabeçalho do `scripts/servidor.sh`, três trechos do `README.md`, docstrings de `tests/test_documentacao.py` e `tests/handshake/conftest.py`, e as mensagens de asserção de `tests/{moodle,jupiter}/test_server_stdio.py`. **As três opções, com custo:** **(a) manter e documentar** — custo de um parágrafo, reversível, e é a que foi implementada em 21/09; deixa o aviso e a pendência na tela de quem instala no Windows, e depende de a pessoa achar o parágrafo. **(b) tirar o `.mcp.json` do repositório** — some o aviso e some a pendência nos três sistemas, e o registro por escopo de usuário com caminho absoluto que o README ensina já é o que de fato conectou na máquina do relator; custa a conveniência de quem mantém e trabalha dentro do clone (que passa a registrar à mão, ou a manter um `.mcp.json` local gitignorado), mais tirar ou reescrever L3 e L4 e emendar a prosa dos nove lugares acima. O `servidor.sh` **não** morre com ela: ele segue sendo o caminho de "cliente que não faz cd" do README e o único jeito de subir o código da árvore em vez do de `site-packages`; o que ele perde é metade da justificativa do próprio cabeçalho. **(c) apontar para algo portátil — não existe forma que sirva.** O `command` do `.mcp.json` é uma string estática única, sem condicional por sistema operacional, e os entry points instalados moram em `.venv/bin/` ou `.venv\Scripts\` conforme o sistema. O Claude Code expande `${VAR}` no arquivo, mas isso é recurso dele e não do protocolo, e exigiria a pessoa definir a variável em cada máquina, que é mais trabalho do que o registro por escopo de usuário que o README já ensina e não ajudaria nenhum outro cliente MCP. Trocar o `command` por `uv run` seria transformar o `uv` em dependência obrigatória (o README oferece `python3 -m venv` como alternativa) e continuaria supondo o cwd na raiz, que é exatamente a premissa que o lançador existe para não transferir ao cliente. **Recomendação de quem levantou isto: (b)**, porque o arquivo cobra de todo mundo que instala pelo README um aviso ou uma pendência, e entrega conveniência só a quem mantém, que é justamente quem sabe registrar à mão. **Não implementada, de propósito: é decisão do dono.** | `.mcp.json`, `tests/test_lancador.py` (L3, L4), `README.md`, `scripts/servidor.sh` | média | 🔵 aberto — (a) implementada em 21/09 como o passo reversível; a escolha entre (a) definitiva e (b) é do dono, e (c) está descartada por impossibilidade e não por preferência |
| 22/09/2026 | Os campos `title` que o pydantic gera em cada `inputSchema` (`"_ja_entregueiArguments"`, `"Disciplina"`) somam **~127 tokens** nos 14 esquemas dos três servidores, pagos em toda sessão, e **não são removíveis por nós**: o SDK deriva o schema da assinatura da função (`usp_mcp/adaptador.py`), então suprimi-los exigiria um gerador de schema próprio — mais superfície do que os 127 tokens valem hoje. Registrado como custo medido que não controlamos, para que a próxima sessão não redescubra o mesmo beco. | `usp_mcp/adaptador.py`, `notas/custo-em-token.md` | baixa | 🔵 aberto |
| 22/09/2026 | `questionarios` é a única ferramenta do Moodle fora do orçamento de `tests/moodle/test_custo.py` por falta de insumo: não há fixture de `mod_quiz_get_quizzes_by_courses` nem de `mod_quiz_get_user_attempts` no repositório. Medir com entrada fabricada seria medir a fabricação (Regra 11). Ela tem a 2ª maior descrição estática do servidor (418 tokens) e custo de resposta desconhecido. Fecha com uma captura higienizada — custa duas chamadas da conta, decisão do dono. | `fixtures/moodle/`, `tests/moodle/test_custo.py` | média | 🔵 aberto |
| 22/09/2026 | **O T58 reprova na máquina do dono, e só nela.** `PARES_CRU` pareia `raw/users_courses.json` com a publicada `users_courses_15-09.json` (47 matrículas), com o comentário afirmando que aquele cru é a captura de 15/09. Nesta máquina ele é a de **31/08, com 74** — e reproduz exatamente a `users_courses.json`, verificado. O cru de 15/09 não existe em `raw/`. Ou seja: o ajuste de 17/09, feito justamente para não comparar duas capturas diferentes, apontou o par para a outra captura diferente. No CI isso é **invisível** (sem `raw/`, T58 pula), então é vermelho só onde alguém tem o cru — a mesma forma de falso-verde que este arquivo persegue, ao contrário. Achado ao rodar o gate para outra tarefa; não consertado para não desviar, e porque mexer no canário da higienização é decisão de privacidade. A cura provável é de uma linha (parear com `users_courses.json`), e quem a fizer precisa dizer o que o par de 15/09 ia cobrir e quem cobre agora. | `tests/moodle/test_higienizacao.py` | alta | 🔵 aberto |
| 22/09/2026 | **O teto de custo do RUCard não alcança a resposta mais cara dele.** O R34 (`tests/rucard/test_custo.py`) trava 4.500 B, medidos em 31/08 sobre "o pior caso (4 RUs × 2 refeições)" — verdade naquele dia. O `dia="semana"` chegou depois, com o R46, e ninguém remediu: a semana inteira dá **8.018 B (2.642 tokens)**, 78% acima do teto, sem nada ficar vermelho. É a maior resposta do projeto inteiro, maior que o `material` do Moodle. Medido em `notas/custo-em-token.md`. A cura é um teto próprio para o caso da semana, não subir o do dia — são duas formas de resposta e um número só esconderia a que cresce. | `tests/rucard/test_custo.py` | alta | 🔵 aberto |
| 22/09/2026 | **O rodapé que iça item comum do `bandejao` falha justamente onde a repetição é pior.** A regra é "comum a TODAS as refeições da resposta": no dia ela pega `Arroz / feijão / arroz integral`; na semana, a interseção de sete blocos × sete dias encolhe a quase nada e o arroz fica inline **32 vezes — 256 tokens, 10% da resposta**. Os itens que aparecem em cinco linhas ou mais somam 429 tokens, 16%. A cura provável é içar **por bloco** (RU × refeição) em vez de por resposta, e ela muda a forma da saída — decisão de desenho, não ajuste. | `usp_mcp/rucard/ferramentas.py` | média | 🔵 aberto |
| 22/09/2026 | **`requisitos` do Jupiter repete um valor constante em toda linha de currículo.** Em MAT2455, `(integral, 3º período ideal)` sai 23 vezes e tem **um único valor distinto**: 230 tokens, 31% da resposta. Içar para o cabeçalho **quando é uniforme, e só então** corta ~30% do pior caso — a condicional importa, porque uma disciplina que apareça em períodos ideais diferentes precisa da informação por linha. Mesmo achado do `[tipo, tamanho, data]` do `material`, medido em `notas/custo-em-token.md`. | `usp_mcp/jupiter/ferramentas.py` | média | 🔵 aberto |
| 22/09/2026 | **`disciplina` do Jupiter repete a própria descrição.** A linha "⚠ Pré-requisito não vem por aqui: use a ferramenta requisitos com a mesma sigla" tem **80% de sobreposição de vocabulário** com a descrição da ferramenta, que o cliente já carrega a sessão inteira — exatamente o limiar do canário OR3 que entrou no Moodle em 22/09. 21 tokens por chamada. É roteamento, e roteamento mora na descrição. | `usp_mcp/jupiter/ferramentas.py` | baixa | 🔵 aberto |
| 22/09/2026 | **O orçamento de saída e o canário de duplicação existem só no Moodle.** `tests/moodle/test_custo.py` (OR1-OR4) mede oito ferramentas com teto por ferramenta, folga máxima de 15% e canário de frase repetida da descrição. Jupiter e RUCard têm testes de custo mais antigos e mais frouxos (teto único por servidor, sem folga máxima, sem canário) — e foi por isso que a resposta da semana do RUCard passou dezenas de commits acima do teto sem ninguém ver. Estender é o que impede a regressão fora do Moodle. | `tests/jupiter/test_custo.py`, `tests/rucard/test_custo.py` | média | 🔵 aberto |
